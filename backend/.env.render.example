# Backend env example for Render
# Replace values in Render dashboard (do NOT commit secrets)
MONGODB_URI=
JWT_SECRET=
CLIENT_ORIGIN=

# Firebase Admin service account pieces (Render: set secrets and join with newlines)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Optional LLM settings
TEXT_LLM_API_KEY=
TEXT_LLM_API_URL=